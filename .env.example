# --- Application -------------------------------------------------------------
APP_NAME=MIOS
APP_VERSION=0.1.0
APP_ENV=development
DEBUG=true

HOST=127.0.0.1
PORT=8000

API_PREFIX=/api/v1

# --- Logging -----------------------------------------------------------------
LOG_LEVEL=INFO
# Structured JSON logs; enable outside local development.
LOG_JSON=false

# --- Startup -----------------------------------------------------------------
# Abort startup when a mandatory infrastructure component is unreachable.
STARTUP_VALIDATION=true

# --- PostgreSQL --------------------------------------------------------------
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_USER=mios
POSTGRES_PASSWORD=mios
POSTGRES_DB=mios

POSTGRES_POOL_SIZE=10
POSTGRES_MAX_OVERFLOW=5
POSTGRES_POOL_TIMEOUT=30
POSTGRES_POOL_RECYCLE=1800
POSTGRES_POOL_PRE_PING=true
POSTGRES_CONNECT_TIMEOUT=10
POSTGRES_ECHO=false

# --- TimescaleDB -------------------------------------------------------------
TIMESCALEDB_ENABLED=true

# --- Redis -------------------------------------------------------------------
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=
REDIS_MAX_CONNECTIONS=20
REDIS_SOCKET_TIMEOUT=5
REDIS_CONNECT_TIMEOUT=5

# --- NATS JetStream ----------------------------------------------------------
# Comma-separated for a cluster.
NATS_SERVERS=nats://127.0.0.1:4222
NATS_CONNECT_TIMEOUT=5
# -1 retries forever.
NATS_MAX_RECONNECT_ATTEMPTS=-1
NATS_RECONNECT_TIME_WAIT=2
NATS_PING_INTERVAL=30
